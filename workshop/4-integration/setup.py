from setuptools import setup, find_packages

setup(
    name="agentspace",
    version="1.0.0",
    description="Agent-Space Architecture",
    author="Andrej Lucny",
    packages=find_packages(),
    python_requires=">=3.5",
)

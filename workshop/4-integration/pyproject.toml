[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentspace"
version = "1.0.0"
description = "Agent-Space Architecture"
authors = [{name = "Andrej Lucny"}]
requires-python = ">=3.5"
